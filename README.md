# personal-website

Personal portfolio — React 19 + TypeScript + Vite + MUI.

## Scripts

- `npm run dev` — local dev server
- `npm run build` — typecheck + production build to `dist/`
- `npm run deploy` — build and publish to GitHub Pages
